using UnityEngine;
using System.Collections;

public class taildamager : MonoBehaviour {
/*
	public bool sendDamage = false;
	public GameObject collision;

	void OnTriggerEnter2D (Collider2D collision)
	{
		if (collision.gameObject.tag == "Player") 
		{ 
		var hit = collision.gameObject;
		var health = hit.GetComponent<SealHealth> ();
			if (health != null) 
			{
//			collision.gameObject.GetComponent<SealHealth> ().TakeDamage (10);
//			health.TakeDamage (5);
			sendDamage = true;
			Debug.Log ("Tail Damager Works!");
			}
//				Destroy (gameObject);
		}
	}
*/
}
