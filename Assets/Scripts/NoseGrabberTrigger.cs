using UnityEngine;
using System.Collections;

public class NoseGrabberTrigger : MonoBehaviour {

/*	public float noseGrabforce = 200;
	
	void OnTriggerEnter2D (Collider2D col)
	{
		if (col.gameObject.tag == "snowball") 
		{
//		rigidbody2D.AddForce (Vector2.up * noseGrabforce, ForceMode2D.Force);
		Debug.Log ("nosegrabber triggered! NOSEGRABBERTRIGGER.CS TRIGGERED");
		}
	}
	*/
}
